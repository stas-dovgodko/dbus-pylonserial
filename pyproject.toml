[project]
name = "dbus-pylontech-console"
version = "0.3.0"
description = "Isolated Pylontech RS232 telemetry service for Victron Venus OS"
requires-python = ">=3.8"
license = {text = "AGPL-3.0-only"}
dependencies = ["pyserial>=3.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
